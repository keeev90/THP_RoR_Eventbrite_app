<nav class="navbar navbar-expand-lg navbar-light bg-light py-3" id="mainNav">
  <div class="container px-4 px-lg-5">
    <a class="navbar-brand" href="<%= root_path %>">Eventbrite</a>
    <% if user_signed_in? && current_user.is_website_admin == true %>
      <a class="nav-item"><%= link_to "Espace admin", admin_root_path, class: "nav-link" %></a>
    <% end %>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ms-auto my-1 my-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to "Créer un évènement", new_event_path, class: "nav-link active" %></li>
          <li class="nav-item">
            <div class="dropdown">
              <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mon profil</button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a><%= link_to "Mon profil", user_path(current_user.id), class: "dropdown-item" %></a>
                <a><%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %></a>
              </div>
            </div>
          </li>
        <% else %>
          <li class="nav-item"><%= link_to "Créer un évènement", new_event_path, class: "nav-link active" %></li>
          <li class="nav-item">
            <div class="dropdown">
              <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Connexion</button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a><%= link_to "Se connecter", new_user_session_path, class: "dropdown-item" %></a>
                <a><%= link_to "S'inscrire", new_user_registration_path, class: "dropdown-item" %></a>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>